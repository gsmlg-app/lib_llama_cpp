## 0.7.0

- Align package metadata with the workspace minor release.

## 0.1.0

- Initial local OpenAI-compatible HTTP server package.
- Added persistent `llcs_engine` wrapper, CLI entrypoint, HTTP routes, and a
  small Dart client.
