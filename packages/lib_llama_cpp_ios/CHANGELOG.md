## Unreleased

- Prefer packaged prebuilt CPU-only iOS xcframeworks when present, with
  source-build fallback for monorepo development.

## 0.1.0

- Initial iOS federated plugin implementation.
- Added native library descriptor registration and iOS FFI plugin metadata.
